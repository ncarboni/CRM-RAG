# Web Framework
Flask>=3.1.1  # Fixed CVE: SECRET_KEY_FALLBACKS signing key order issue
Werkzeug>=3.1.4  # Fixed CVE: Windows device names path traversal vulnerability

# SPARQL and RDF
SPARQLWrapper==2.0.0
rdflib==7.1.1

# LangChain and RAG
langchain-core>=0.3.0  # Core components (Document class, etc.)
langchain-community>=0.3.27  # Minimum version to fix XXE vulnerability (CVE)
langchain-openai
langchain-anthropic

# Vector Store
faiss-cpu==1.9.0.post1

# Local Embeddings (Optional - for fast batch embedding without API)
# Install with: pip install sentence-transformers torch
# These are optional - the system works without them using API embeddings
sentence-transformers>=2.2.0
torch>=2.0.0  # Includes CUDA support when available

# Graph Processing
networkx>=3.0

# Numerical Computing
numpy>=1.24.0

# Utilities
python-dotenv==1.0.1
tqdm==4.67.1
requests>=2.32.4  # Fixed CVE: .netrc credential leak vulnerability
PyYAML==6.0.2  # For loading interface configuration

# Optional: Development Tools
# pytest==8.3.4
# black==24.10.0
# flake8==7.1.1
